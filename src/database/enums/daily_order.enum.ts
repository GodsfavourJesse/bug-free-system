export enum DailyOrderStatus {
    PENDING = "pending",
    IN_PROGRESS = "in_progress",
    COMPLETED = "completed",
    EXPIRED = "expired",
}

export enum DailyOrderItemStatus {
    PENDING = "pending",
    COMPLETED = "completed",
    EXPIRED = "expired",
}